请编写一个页面，包含一个用键盘方向键控制移动的彩色方块，以及一个可以用鼠标拖拽移动的矩形区域。所有交互必须通过事件监听实现。

🎯 功能需求（必须全部实现）
序号	功能	具体要求
①	键盘移动方块	页面中有一个固定大小的彩色方块，按下键盘的 ↑ ↓ ← → 方向键时，方块向对应方向移动一段距离（如 20px），且不能移出浏览器可视区域
②	平滑移动	方块的移动应有过渡或视觉上的平滑感（提示：使用 CSS transition 或 JS 动画）
③	鼠标拖拽 div	页面中有另一个矩形区域，用户可以用鼠标按住它并拖动到页面任意位置，松开鼠标后停留在新位置
④	拖拽边界限制	被拖拽的 div 不允许完全拖出可视区域（至少保留一部分在屏幕内）
⑤	样式区分	键盘移动的方块和鼠标拖拽的 div 在颜色、大小、边框上有明显区分，让人一眼就知道哪个是键盘控制、哪个是鼠标控制

你需要自己完成：

HTML 中添加两个元素：一个是键盘移动的方块，一个是鼠标拖拽的 div

给它们设置不同的 id 或 class，方便选择

🔧 技术实现要点
键盘移动方块
使用 keydown 事件监听全局按键

判断 event.key 是否为 ArrowUp、ArrowDown、ArrowLeft、ArrowRight

修改方块元素的 style.top / style.left（元素需设置 position: absolute 或 fixed）

注意边界判断：方块坐标不能小于 0，不能大于窗口宽高减方块自身尺寸

建议给方块加上 transition: top 0.1s, left 0.1s; 实现平滑移动

鼠标拖拽 div
使用 mousedown、mousemove、mouseup 三个事件组合

在 mousedown 时记录鼠标在元素上的偏移量（offsetX、offsetY），并打开拖拽状态

在 mousemove 时（仅当拖拽状态为 true），根据鼠标位置和偏移量计算元素新坐标，更新 style.left 和 style.top

在 mouseup 时关闭拖拽状态

注意：mousemove 和 mouseup 应绑定在 document 上，而不是元素本身，防止鼠标移动过快时脱靶

边界限制：拖拽时计算元素的新位置，不允许完全移到窗口外（保留至少 20px 在可视区域内）

🎨 样式建议（自由发挥）
键盘方块：80×80px，鲜艳背景色（如 coral），固定定位或绝对定位，有 border-radius 和阴影

拖拽 div：120×80px 或更大，不同颜色（如 dodgerblue），显示文字「拖我」，鼠标移上去变 grab 光标，拖拽时变 grabbing

可以在页面加一点说明文字，提示操作方式