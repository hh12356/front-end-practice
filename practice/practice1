1. HTML 结构

一个控制面板区域（可放在顶部）

4 个改变背景色的按钮（红、蓝、绿、白）

一个「随机换色」按钮

一个文本输入框 + 一个「修改标题」按钮

一个「添加卡片」按钮

一个内容展示区，内含一个可被修改的标题（如 <h1>）

卡片动态添加区（最初为空）

2. CSS 样式

自行设计布局和美化（可以简单，但起码要能区分按钮、卡片）

背景色切换时建议加 transition 过渡效果

卡片建议有边框或阴影，删除按钮放在卡片内部

3. JavaScript 功能

严格完成这 5 个功能，缺一不可：

换背景色 – 点击颜色按钮或随机按钮，改变 body 背景色

修改标题 – 取输入框的值，更新展示区标题；空值时 alert 提示

添加卡片 – 动态创建带有序号和删除按钮的卡片，插入展示区

删除卡片 – 必须使用事件委托，在父容器上监听点击，判断目标是否为删除按钮，然后移除对应卡片

随机背景 – 生成随机十六进制颜色并设置

🧭 建议的实现顺序

先写出所有 HTML 元素（不写 JS），确保页面能看到按钮和输入框

写 CSS，把布局弄整齐

再写 JS：

获取所有需要的 DOM 元素

实现颜色按钮的事件绑定

实现随机颜色

实现修改标题

实现添加卡片（记一个卡片序号变量）

最后用事件委托处理删除

📌 提醒

不要用 innerHTML 直接拼接字符串来添加卡片（不安全且事件委托易失效），推荐用 document.createElement 或 insertAdjacentHTML 配合事件委托。

删除按钮的 click 事件不要直接绑在按钮上，要在父容器上统一处理。