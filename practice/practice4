题目概述

编写一个页面，包含一个大按钮。根据用户点击次数，按钮会呈现不同的颜色和文字：

点 1 次（单击）→ 按钮变红色，文字显示“红了”

点 2 次（在一定时间内快速连点）→ 按钮变蓝色，文字显示“蓝了”

点 3 次（同样快速连点）→ 按钮恢复初始颜色和文字

核心挑战：浏览器只提供 click 和 dblclick 事件，你需要自己实现单击 / 双击 / 三击的区分，并且用状态管理来记录当前是第几次点击。

🎯 功能需求（必须全部实现）

初始状态
按钮背景为灰色或白色，文字显示“点我试试”。

单击（点一下）
点击按钮后，按钮背景变为红色，文字变为“红了”。

双击（快速点两下）
在红色状态下，如果在 500ms 内再次点击，按钮背景变为蓝色，文字变为“蓝了”。

如果两次点击间隔超过 500ms，则第二次点击视为一次新的“单击”，按钮保持红色（或重新变红）。

三击（快速点三下）
在蓝色状态下，如果在 500ms 内再次点击，按钮恢复初始状态（灰色背景，“点我试试”）。

循环
恢复初始后，再次点击又可以重复 红→蓝→灰 的循环。

视觉反馈
按钮应有明显的颜色过渡（CSS transition），且鼠标悬停时有小手光标。

技术实现要点
这是一个状态机问题。推荐思路：

定义状态

state = 0：初始（灰色）

state = 1：红色（单击后）

state = 2：蓝色（双击后）

点击处理流程

每次 click 事件触发时，启动（或重置）一个 500ms 定时器。

在定时器到期前，如果没有再次点击，则根据当前状态执行“单击”对应的动作（变红/变蓝/复原）。

如果定时器到期前再次点击，说明用户是快速连点，则取消之前的定时器，状态递增（1→2→0→1…），然后重新启动定时器等待下一次可能连击。

如果你先单击等待超时（变红），然后又单击，这应该变成双击吗？
按题目要求，第二次单击如果距离第一次超过 500ms，就应该是一次新的“单击”，按钮应该保持红色（因为状态从 0 变 1 后就停止了）。
上面的逻辑可以实现：定时器超时后 clickTimer 置 null，
下次点击会重新开始状态从当前值+1，例如当前是红(1)，点击变蓝(2)，符合预期。