接下来是第三个任务的详细题目，同样从零搭建。

📋 题目概述

编写一个页面，包含：

一个倒计时器：用户设定时间，点击开始后倒计时，到零时提醒。

一个自动轮播图：至少 3 张图片循环切换，支持自动播放和手动切换。

🎯 功能需求

倒计时器（使用 setInterval / setTimeout）

一个输入框让用户输入秒数（正整数）

一个显示区域，实时显示剩余秒数（格式：MM:SS）

三个按钮：「开始」「暂停」「重置」

点「开始」：从输入秒数开始倒计时，每秒减 1，按钮变为不可点击或隐藏

点「暂停」：暂停倒计时，保留剩余秒数，按钮文字变为「继续」

点「继续」：从暂停处继续倒计时

倒计时到 0 时：显示 "时间到！"，并播放一个提示（如弹窗或变色）

输入校验：非正整数时提示错误

轮播图（使用 setInterval）

至少 3 张图片（可以用占位图服务，如 https://picsum.photos/600/300?random=1 等）

图片区域固定大小，自动每 2 秒切换到下一张

下方有小圆点指示器，显示当前是第几张

两个箭头按钮：左箭头（上一张）、右箭头（下一张），点击可手动切换

鼠标悬停轮播区域时暂停自动播放，移开后恢复

支持无限循环：最后一张下一张回到第一张；第一张上一张跳到最后一张

切换时有淡入淡出或滑动效果（用 CSS transition 即可）